# Production environment variables for the Vite frontend build
# When both frontend and backend are served from the same domain (app.vstepconsultancy.com),
# leave VITE_API_BASE_URL empty so API calls use the same origin automatically.
VITE_API_BASE_URL=
